export type postType = {
  title: string;
  slug: string;
  eyecatch: {
    url: string;
    blurDataURL: string;
  };
};

export type categoryType = {
  name: string;
  slug: string;
};

export type languageType = {
  name: string;
  experience: string;
  detail: string;
}